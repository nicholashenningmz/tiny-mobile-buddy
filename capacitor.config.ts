

import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.phoenicks.evilchoozi',
  appName: 'Evil Choozi',
  webDir: 'dist',

  bundledWebRuntime: false
};

export default config;
